# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## 发布检查

发版前请确认：

1. 更新 `scripts/version.py` 中的 `VERSION` 为新版本号
2. 全局搜索 `v\d+\.\d+`，更新所有静态显示文本中的版本号
3. 为本版本新增 CHANGELOG 条目，记录新增/变更/修复
4. 运行版本一致性检查：
   ```powershell
   $v = (Select-String -Path scripts/version.py -Pattern 'VERSION = "(\d+\.\d+)"').Matches.Groups[1].Value
   Select-String -Path README.md -Pattern "v$v" -Quiet
   Select-String -Path scripts/webui/static/index.html -Pattern "v$v" -Quiet
   Select-String -Path CHANGELOG.md -Pattern "\[$v\]" -Quiet
   ```
   所有输出为 `True` 即通过。
5. 确认 `git diff --check` 无空白错误

---

## [Unreleased]

---

## [4.4] — 2026-07-30

### 变更
- **版本升级**：版本号 4.3 → 4.4。
- **疑似麻友识别优化**：默认分析时段调整为 20:00 至次日 02:00，并支持在 GUI 中独立配置。
- **固定圈子分析**：识别重点从单个对手方跨夜重复，调整为多个重复对手方在夜间 session 中共同出现。
- **金额统计口径**：金额不作为命中或排除条件，仅用于总金额、最高单笔、单晚最高、平均每晚金额等统计字段。

### 新增
- **特殊交易 GUI 配置**：支持独立调整特殊日期、金额模式、备注2关键词和对手侧账户名称关键词，配置可跨程序重启持久化。
- **特殊对手方筛选**：可按对手侧账户名称的自定义关键词筛选关注交易，默认启用并预置桑拿、足浴、休闲、会所等关注关键词。
- **疑似麻友-圈子统计**：新增独立工作表，展示固定交易圈子的共同出现晚数、交易笔数和金额统计。

---

## [4.3] — 2026-06-17

### 新增
- **停车缴费地点识别（AI）**：调用 DeepSeek API 从停车备注文本中提取停车场/商场/小区名称
  - 7 种常见备注格式 × 30+ few-shot 示例，覆盖国内停车场景
  - 去重后分块并发（≤100 条/块，8 线程），数百条备注 10 秒内完成
  - 已有非"无"地点值保留不覆盖（保护人工修正）
  - 可选功能，API Key 通过 GUI 输入、内存存储，失败不影响主流程输出
- **`scripts/core/location.py`**：DeepSeek 地点提取模块（仅 `urllib` 标准库，零新增外部依赖）
- **GUI 地点识别**：复选框 + API Key 输入面板 + 事后重新提取按钮

### 变更
- 版本号 4.2 → 4.3

---

## [4.2] — 2026-06-14

### 新增
- **群红包识别**：检测同日同时向 ≥2 人发送微信红包的行为，统计对手方收款
  - 新建 `scripts/core/group_red_packet.py`
  - 输出「群红包记录」和「群红包-统计」两个 Excel sheet
  - 集成到 `post_merge_analysis()` 共享分析管线（CLI 合并、GUI 合并、单批次三条路径一致）

### 修正
- **去重逻辑**：8 列联合主键（用户 ID + 交易单号 + 大单号 + 日期 + 时间 + 借贷类型 + 交易金额 + 对手方 ID）替代自适应降级策略
  - 修复群红包场景误删：群发红包时多条记录的前 7 列完全相同，仅对手方/金额不同，旧逻辑会删剩 1 条

### 变更
- 群红包统计排序由金额倒序改为次数倒序
- CLI/GUI/前端全路径展示制作人（`AUTHOR = "钟建成"`）
- 版本号 4.1 → 4.2

---

## [4.1] — 2026-06-11

### 新增
- **注册信息提取合并**：遍历 `TenpayRegInfo.txt`，提取账户状态、身份信息、变更历史并合并去重
  - CLI 入口 `app_reg.py`，支持 `-s -o -n` 参数
  - 核心模块 `reg_reader.py`（两区域格式解析 + 银行卡扩展行 + 账号不存在特殊格式）
  - 核心模块 `reg_processor.py`（按 账号+身份证号 去重 + 变更类型分类 + 自然人基础信息提取）
  - 输出三 sheet Excel：注册信息汇总 / 变更记录 / 基础信息
  - 覆盖 4 种账户状态：正常 / 已注销 / 未注册 / 账号不存在
- **GUI 注册信息清洗集成**：单批次清洗页新增「同时清洗注册信息」勾选框（默认勾选）
  - 复用交易流水已选的输入/输出目录，零额外配置
  - 文件名自动加时间戳 `MMDD_HHmm`（如 `Tenpay_merge_0611_1532.xlsx`）
  - 交易流水和注册信息清洗结果分卡片展示
- **集中版本号管理**：`scripts/version.py` 统一定义，Python 入口动态读取，前端通过 bridge API 获取
- **CHANGELOG.md**：本文件

### 修复
- **pywebview 6.x 文件对话框失效**：bridge 异步注入导致 `window.pywebview.api` 为 undefined，降级到浏览器 `prompt()` 弹窗。改用 `pywebviewready` 事件 + 动态 API 检测
- **注册信息日志不显示**：交易流水完成后 status="done" 导致前端停止轮询，注册信息处理日志被忽略。修复为注册信息期间保持 `running` 状态

### 变更
- 文件名默认加时间戳后缀（打开 GUI 即显示），点击开始处理时刷新为最新时刻
- 勾选框标签动态显示注册信息输出文件名

---

## [4.0] — 2026-06（初始版本）

### 功能
- 单批次交易流水清洗（CLI + GUI）
- 多批次合并去重（联合主键 交易单号 + 大单号）
- 自适应列名识别（关键词匹配，不硬编码位置）
- 金额分转元、时间拆分、进账/出账分类
- 时段分类（凌晨/早上/下午/晚上，可配置）
- 日期分类（工作日/节假日/周末，依赖 chinesecalendar）
- 停车缴费识别 + 车牌提取（正则匹配省份简称）
- 特殊交易筛选（情感数字/特殊日期/特殊备注）
- Excel 多 sheet 输出（汇总 + 停车缴费 + 特殊交易）
- GUI 配置编辑（停车/时段/特殊交易筛选规则）
- GUI 日志面板（含复制按钮、进度条）
- pywebview + React 前端（零 CDN 依赖）

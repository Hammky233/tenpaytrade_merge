# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [4.1] — 2026-06-11

### 新增
- **注册信息提取合并**：遍历 `TenpayRegInfo.txt`，提取账户状态、身份信息、变更历史并合并去重
  - CLI 入口 `app_reg.py`，支持 `-s -o -n` 参数
  - 核心模块 `reg_reader.py`（两区域格式解析 + 银行卡扩展行 + 账号不存在特殊格式）
  - 核心模块 `reg_processor.py`（按 账号+身份证号 去重 + 变更类型分类 + 自然人基础信息提取）
  - 输出三 sheet Excel：注册信息汇总 / 变更记录 / 基础信息
  - 覆盖 4 种账户状态：正常 / 已注销 / 未注册 / 账号不存在
- **GUI 注册信息清洗集成**：单批次清洗页新增「同时清洗注册信息」勾选框（默认勾选）
  - 复用交易流水已选的输入/输出目录，零额外配置
  - 文件名自动加时间戳 `MMDD_HHmm`（如 `Tenpay_merge_0611_1532.xlsx`）
  - 交易流水和注册信息清洗结果分卡片展示
- **集中版本号管理**：`scripts/version.py` 统一定义，Python 入口动态读取，前端通过 bridge API 获取
- **CHANGELOG.md**：本文件

### 修复
- **pywebview 6.x 文件对话框失效**：bridge 异步注入导致 `window.pywebview.api` 为 undefined，降级到浏览器 `prompt()` 弹窗。改用 `pywebviewready` 事件 + 动态 API 检测
- **注册信息日志不显示**：交易流水完成后 status="done" 导致前端停止轮询，注册信息处理日志被忽略。修复为注册信息期间保持 `running` 状态

### 变更
- 文件名默认加时间戳后缀（打开 GUI 即显示），点击开始处理时刷新为最新时刻
- 勾选框标签动态显示注册信息输出文件名

---

## [4.0] — 2026-06（初始版本）

### 功能
- 单批次交易流水清洗（CLI + GUI）
- 多批次合并去重（联合主键 交易单号 + 大单号）
- 自适应列名识别（关键词匹配，不硬编码位置）
- 金额分转元、时间拆分、进账/出账分类
- 时段分类（凌晨/早上/下午/晚上，可配置）
- 日期分类（工作日/节假日/周末，依赖 chinesecalendar）
- 停车缴费识别 + 车牌提取（正则匹配省份简称）
- 特殊交易筛选（情感数字/特殊日期/特殊备注）
- Excel 多 sheet 输出（汇总 + 停车缴费 + 特殊交易）
- GUI 配置编辑（停车/时段/特殊交易筛选规则）
- GUI 日志面板（含复制按钮、进度条）
- pywebview + React 前端（零 CDN 依赖）

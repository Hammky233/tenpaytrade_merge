"""
pytest 共享 fixtures

将 `scripts/` 加入 sys.path，使测试文件可以直接导入 core、utils 等模块。
"""

import sys
import pytest
from pathlib import Path

# 将项目 scripts 目录加入导入路径
_SCRIPTS_DIR = Path(__file__).resolve().parent.parent / "scripts"
if str(_SCRIPTS_DIR) not in sys.path:
    sys.path.insert(0, str(_SCRIPTS_DIR))


# ============================================================================
# 共享 fixture：示例 DataFrame
# ============================================================================

@pytest.fixture
def sample_trades_df():
    """一个 5 行的最小交易流水 DataFrame，含全部必要列。"""
    import pandas as pd
    return pd.DataFrame({
        "交易时间": [
            "2026/4/2 14:30:00",
            "2026/4/2 8:15:00",
            "2026/4/3 22:10:00",
            "2026/4/5 0:05:00",
            "2026/4/6 6:00:00",
        ],
        "交易用途类型": ["转账", "消费", "转账", "红包", "转账"],
        "借贷类型": ["入", "出", "出", "出", "入"],
        "交易金额(分)": ["10000", "5000", "20000", "100", "30000"],
        "用户ID": ["U001", "U001", "U002", "U001", "U003"],
        "交易单号": ["T001", "T002", "T003", "T004", "T005"],
        "大单号": ["B001", "B002", "B003", "B004", "B005"],
        "备注1": ["", "", "", "微信红包", ""],
        "备注2": ["", "停车缴费", "", "", ""],
        "对手方ID": ["P001", "P002", "P003", "P004", "P005"],
        "对手方接收金额(元)": [100.0, 50.0, 200.0, 0.50, 300.0],
        "用户侧账号名称": ["用户A", "用户A", "用户B", "用户A", "用户C"],
        "对手侧账户名称": ["商户X", "停车场Y", "商户Z", "朋友D", "商户W"],
    })


@pytest.fixture
def time_period_config():
    """默认时段配置字典。"""
    return {
        "时段": [
            {"name": "凌晨", "start": "00:00", "end": "06:00"},
            {"name": "早上", "start": "06:00", "end": "12:00"},
            {"name": "下午", "start": "12:00", "end": "19:00"},
            {"name": "晚上", "start": "19:00", "end": "24:00"},
        ],
    }
